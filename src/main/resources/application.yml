server:
  port: 8081

spring:
  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/xe?oracle.jdbc.timezone=Asia/Taipei
    username: JPA
    password: springboot
    driver-class-name: oracle.jdbc.driver.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
